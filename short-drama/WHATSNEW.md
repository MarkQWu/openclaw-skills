**v1.31.9**（2026-05-11）

新增 `references/reference-layers.md`，作为维护者使用的规则分层索引，先把现有 references 归到 foundation / structure / craft / modes / templates，后续用于 hard-gate metadata 和 quality rubric 重构。不改变用户命令行为，不迁移 runtime。

输入 `/帮助` 查看全部命令

---

**v1.31.8**（2026-05-11）

同步 short-drama-remake v0.4.0 的 `/仿写` 子命令入口说明。

- `/仿写` 仍是兼容跳转入口，真正拆解复刻能力在同级 `short-drama-remake`。
- 新增用户可见入口提示：`/仿写 帮助`、`/仿写 状态 PROJECT_DIR`、`/仿写 继续 PROJECT_DIR`。
- 明确原创主流程 `/开始` 和复刻项目恢复入口分开，避免新对话恢复时误进原创项目列表。

**v1.31.7**（2026-05-11）

修复 WorkBuddy 中 `/更新` 被误当成“更新项目状态”的路由问题。现在 `/更新` 被定义为最高优先级精确命令，只能执行仓库级 skill 更新，不得生成剧本项目状态表或推进当前剧本项目。

---

**v1.31.6**（2026-05-10）

安装指引改为 git + 全局代理路线。若安装器检测不到 git，会提示先安装 Git；若 GitHub 下载失败，会提示打开全局代理后重新运行安装命令。

---

**v1.31.5**（2026-05-10）

历史说明：此版本曾短暂尝试另一种下载策略；v1.31.6 起安装指引已改回 git + 全局代理路线。

---

**v1.31.4**（2026-05-09）

`/更新` 已改为仓库级更新：会从 `drama-workshop-skills` 拉取最新版并重新安装仓库内全部 sibling skills，包括 `short-drama` 和 `short-drama-remake`。同时清理旧仓库名引用。

---

**v1.31.3**（2026-05-08）

WorkBuddy 安装修复：安装脚本会自动把 `skills/.trash` 迁移到同级 `.skill-trash/`，避免 WorkBuddy 递归扫描旧版 `short-drama/SKILL.md`，导致 `/仿写` 仍进入旧流程。重装后请关闭/移除当前 WorkBuddy 工作空间项目并重新打开。

---

**v1.31.2**（2026-05-08）

`/仿写` 兼容入口修复：用户继续输入 `/仿写` 时，不再进入旧项目扫描流程；会优先加载同级 `short-drama-remake`，直接执行新版短剧拆解复刻能力。只有未安装 `short-drama-remake` 时，才提示重装最新版短剧 Skill。

---

**v1.31.1**（2026-05-07）

升级提醒加强：输入 `/开始`、`/更新` 或任意短剧命令时，若检测到旧版本，会提醒更新。历史说明：此版本发布时，旧安装器需要重装才能补齐新版独立 `/仿写` 能力；v1.31.4 起 `/更新` 已升级为仓库级更新。

请让你的智能体执行：
“帮我重装最新版短剧 Skill，并确认新版仿写能力已安装成功。”

剧本正文破折号完全禁用：`——`、`—`、`--` 任意出现均标【严重】。停顿、被打断、补充说明、心理独白引出，改用动作描写、换对话轮次、完整句、句号、逗号或中文省略号「…」。

---

**v1.31.0**（2026-05-07）

新版 `/仿写` 已升级为独立的短剧拆解复刻能力，重点支持长剧本拆分、可复刻骨架、10 个换皮方向、项目策划、详细集纲和正式短剧剧本。

历史说明：此版本发布时需要重装才能补齐新增能力；v1.31.4 起 `/更新` 已升级为仓库级更新。若仍缺少 `short-drama-remake`，请让你的智能体执行：
“帮我重装最新版短剧 Skill，并确认新版仿写能力已安装成功。”

安装完成后，关闭当前会话并重新打开，再继续使用新版 `/仿写`。

---

**v1.30.0**（2026-05-03）

新增大文件仿写支持：50集以上的源剧本现在会自动压缩处理，无需手动拆分提供

---

**v1.28.2**

新增 `/新功能` 命令：随时查看最近一次更新了什么

---

输入 `/帮助` 查看全部命令
